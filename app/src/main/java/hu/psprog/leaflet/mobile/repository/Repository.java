package hu.psprog.leaflet.mobile.repository;

/**
 * Marker interface for repositories, so implementations can be collected for Dagger DI configuration.
 *
 * @author Peter Smith
 */
public interface Repository {
}
